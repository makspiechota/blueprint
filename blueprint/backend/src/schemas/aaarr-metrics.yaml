type: aaarr-metrics
productName: string
version: string
last_updated: string
title: string
lean_viability_ref: string
stages:
  acquisition:
    stage_goal: string
    metrics:
      - id: string
        name: string
        description: string
        target:
          rate?: number
          period?: string
          percentage?: number
          count?: number
          amount?: number
          currency?: string
  activation:
    stage_goal: string
    metrics:
      - id: string
        name: string
        description: string
        target:
          rate?: number
          period?: string
          percentage?: number
          count?: number
          amount?: number
          currency?: string
  retention:
    stage_goal: string
    metrics:
      - id: string
        name: string
        description: string
        target:
          rate?: number
          period?: string
          percentage?: number
          count?: number
          amount?: number
          currency?: string
  referral:
    stage_goal: string
    metrics:
      - id: string
        name: string
        description: string
        target:
          rate?: number
          period?: string
          percentage?: number
          count?: number
          amount?: number
          currency?: string
  revenue:
    stage_goal: string
    metrics:
      - id: string
        name: string
        description: string
        target:
          rate?: number
          period?: string
          percentage?: number
          count?: number
          amount?: number
          currency?: string